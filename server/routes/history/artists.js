import express from 'express';
import db from './database/db.js';

//Get all artists listened to on a spcific date
app.get('/artist', async (req, res) => {
  // take in artist name
  // return all songs listened to by this artist on the date.
});
