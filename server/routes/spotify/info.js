import express from 'express';

app.get('/songInfo', async (req, res) => {
  //take in song id, return song info.
});

app.get('/artistInfo', async (req, res) => {
  //take in artist id (or name?), return artist info.
});
